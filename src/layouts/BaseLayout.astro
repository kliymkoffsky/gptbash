---
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;

import quotesData from '../data/quotes.json';

// Calculate real stats from the data
const totalQuotes = quotesData.quotes.length;
const totalRejected = quotesData.meta.totalRejected;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="alternate" type="application/rss+xml" href="/rss.xml" title="gptbash.com RSS" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <!-- Header -->
    <h1 id="head">
      <a href="/">bash.org.pl</a>
    </h1>

    <!-- Main wrapper with white background -->
    <div id="main">
      <!-- Navigation Menu - positioned top right like original -->
      <div id="menu">
        <a href="/latest/">latest</a>
        <a href="/top/">top</a>
        <a href="/random/">random</a>
        <a href="/add/">submit</a>
        <a href="/rss.xml">rss</a>
      </div>

      <!-- Content -->
      <div id="content">
        <slot />
      </div>
    </div>

    <!-- Footer with real stats -->
    <div id="foot">
      <strong>{totalQuotes.toLocaleString('en-US')}</strong> accepted quotes,
      <strong>{totalRejected.toLocaleString('en-US')}</strong> rejected,
      <a href="/privacy/">privacy policy</a>
    </div>
  </body>
</html>
